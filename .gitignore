# Generated data tables — deterministic from config.yaml + generator.py (§23
# seeded per business). Regenerate with `python generator.py`. Not committed:
# transactions.csv alone runs ~720MB at full scale (N=11,000), well past
# GitHub's 100MB per-file limit.
*.csv
!sources/**/*.csv

# Generated plots (regenerate with `python gate_week3.py`)
ramadan_pos_check*.png

# Quick-mode / scratch output directories
quick_out/
/out/

# Pre-git manual snapshot of the generator taken before the schema-alignment
# rewrite (2026-09-14). Superseded by git history now that this is a repo —
# kept locally for reference, not part of the tracked project.
_backup_pre_schema_align/

# Python
__pycache__/
*.pyc
.ruff_cache/
*.egg-info/
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
