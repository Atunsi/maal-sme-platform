"""Evaluation harnesses — gates and validation that run against generator OUTPUT only.

Held-out anomaly types (§12) live here and must never appear in config.yaml or
the generator package. See eval/README.md.
"""
